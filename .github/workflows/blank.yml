name: demo-workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          echo "this is first line"
          echo "this is second line"
    
    
