name: outputs_demo

on:
  workflow_dispatch:

jobs:
  outputs:
    runs-on: ubuntu-22.04
    steps:
      - name: step-1
        id: step-1
        run: echo version=0.1 >> $GITHUB_OUTPUT

      - name: step-2
        id: step-2
        run: echo ${{steps.step-1.outputs.version}}
          
